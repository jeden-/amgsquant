# Cursor Task: task_20251017_195318_775460

**Created:** 2025-10-17 19:53:18
**Project:** /Users/mariusz/amgsquant

## Task Instructions

CLEANUP I INICJALIZACJA PROJEKTU DRUKARNI WIELKOFORMATOWEJ

KROK 1: Cleanup starych plików
1. Usuń wszystkie stare pliki z poprzedniego projektu (client/, server/, docs/legal_requirements.md, wszystkie .md poza tym co stworzymy)
2. Zachowaj tylko folder .git/ i .vscode/

KROK 2: Inicjalizacja Git
```bash
cd /Users/mariusz/amgsquant
git checkout -b develop
git remote set-url origin https://github.com/jeden-/amgsquant.git
```

KROK 3: Stwórz strukturę główną projektu
```
/Users/mariusz/amgsquant/
├── .github/
│   └── workflows/
│       └── ci.yml
├── apps/
│   ├── frontend/
│   ├── backend/
│   └── admin/
├── packages/
│   ├── database/
│   ├── shared/
│   └── config/
├── docker/
│   ├── nginx/
│   ├── postgres/
│   └── redis/
├── docs/
│   ├── api/
│   ├── architecture/
│   └── deployment/
├── scripts/
├── .cursorrules
├── .gitignore
├── .env.example
├── docker-compose.yml
├── docker-compose.dev.yml
├── package.json (root - pnpm workspace)
├── pnpm-workspace.yaml
├── turbo.json
└── README.md
```

KROK 4: Stwórz plik .gitignore
```
# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
dist/
build/
.next/
out/

# Misc
.DS_Store
*.pem
.env
.env.local
.env.production.local
.env.development.local
.env.test.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Turborepo
.turbo

# Prisma
packages/database/prisma/migrations/
*.db
*.db-journal

# IDEs
.idea/
.vscode/settings.json
.vscode/launch.json
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
```

KROK 5: Stwórz root package.json z pnpm workspaces
```json
{
  "name": "amgsquant-print-system",
  "version": "1.0.0",
  "private": true,
  "description": "System zarządzania drukarnią wielkoformatową - Sklep + Zarządzanie produkcją",
  "author": "AMGSquant",
  "license": "UNLICENSED",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules",
    "docker:up": "docker-compose -f docker-compose.dev.yml up -d",
    "docker:down": "docker-compose -f docker-compose.dev.yml down",
    "db:migrate": "pnpm --filter @amgsquant/database migrate",
    "db:seed": "pnpm --filter @amgsquant/database seed",
    "db:studio": "pnpm --filter @amgsquant/database studio"
  },
  "devDependencies": {
    "turbo": "^1.11.0",
    "prettier": "^3.1.0",
    "eslint": "^8.55.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
```

KROK 6: Stwórz pnpm-workspace.yaml
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

KROK 7: Stwórz turbo.json
```json
{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "build/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "test": {
      "dependsOn": ["^test"]
    },
    "clean": {
      "cache": false
    }
  }
}
```

KROK 8: Stwórz README.md
```markdown
# AMGSquant Print System - System Zarządzania Drukarnią Wielkoformatową

## 🎯 Opis Projektu

Kompleksowy system e-commerce + zarządzanie produkcją dla drukarni wielkoformatowej.

### Kluczowe funkcjonalności:
- 🛒 Sklep internetowy z konfiguratorem produktów
- 💰 Automatyczna wycena w czasie rzeczywistym
- 📁 Walidacja plików graficznych (preflight)
- 🏭 Zarządzanie produkcją i kolejkowanie zadań
- 📦 Zarządzanie magazynem materiałów
- 🚚 Integracja z kurierami
- 📊 Panele dla różnych typów użytkowników (B2C, B2B, Agencje)

## 🏗️ Architektura

**Monorepo** z wykorzystaniem:
- **Frontend**: Next.js 14 (App Router) + TypeScript + Tailwind CSS
- **Backend**: NestJS + Prisma + PostgreSQL
- **Cache**: Redis
- **Queue**: RabbitMQ
- **Storage**: MinIO (S3-compatible)

## 🚀 Quick Start

### Wymagania
- Node.js >= 18
- pnpm >= 8
- Docker & Docker Compose

### Instalacja

1. Clone repository:
\`\`\`bash
git clone https://github.com/jeden-/amgsquant.git
cd amgsquant
\`\`\`

2. Zainstaluj zależności:
\`\`\`bash
pnpm install
\`\`\`

3. Uruchom infrastrukturę (Docker):
\`\`\`bash
pnpm docker:up
\`\`\`

4. Skonfiguruj zmienne środowiskowe:
\`\`\`bash
cp .env.example .env
# Edytuj .env
\`\`\`

5. Uruchom migracje bazy danych:
\`\`\`bash
pnpm db:migrate
pnpm db:seed
\`\`\`

6. Uruchom aplikację w trybie dev:
\`\`\`bash
pnpm dev
\`\`\`

Aplikacja dostępna pod:
- Frontend: http://localhost:3000
- Backend API: http://localhost:4000
- Admin Panel: http://localhost:3001
- Prisma Studio: http://localhost:5555

## 📚 Dokumentacja

- [Architektura systemu](./docs/architecture/)
- [API Documentation](./docs/api/)
- [Deployment Guide](./docs/deployment/)

## 🔧 Dostępne komendy

\`\`\`bash
pnpm dev          # Uruchom wszystkie aplikacje w trybie dev
pnpm build        # Build wszystkich aplikacji
pnpm lint         # Linting
pnpm test         # Testy
pnpm docker:up    # Uruchom infrastrukturę
pnpm docker:down  # Zatrzymaj infrastrukturę
pnpm db:migrate   # Migracje bazy danych
pnpm db:seed      # Seed danych testowych
pnpm db:studio    # Prisma Studio
\`\`\`

## 📦 Struktura Projektu

\`\`\`
apps/
├── frontend/     # Next.js - sklep dla klientów
├── backend/      # NestJS - API
└── admin/        # Panel administracyjny

packages/
├── database/     # Prisma schema + migracje
├── shared/       # Współdzielone typy TypeScript
└── config/       # Współdzielona konfiguracja

docker/           # Docker configs
docs/             # Dokumentacja
scripts/          # Utility scripts
\`\`\`

## 👥 Team

- Project Lead: [Twoje Imię]
- Backend: NestJS + Prisma
- Frontend: Next.js + React
- DevOps: Docker + GitHub Actions

## 📄 Licencja

Proprietary - All rights reserved
\`\`\`

KROK 9: Stwórz .env.example
```bash
# Database
DATABASE_URL="postgresql://printuser:printpass@localhost:5432/printdb"

# Redis
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_PASSWORD=""

# RabbitMQ
RABBITMQ_URL="amqp://guest:guest@localhost:5672"

# MinIO / S3
MINIO_ENDPOINT="localhost"
MINIO_PORT=9000
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_BUCKET_FILES="print-files"
MINIO_BUCKET_THUMBNAILS="print-thumbnails"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRATION="7d"

# Backend
BACKEND_PORT=4000
BACKEND_URL="http://localhost:4000"

# Frontend
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Payment (Stripe/PayU)
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""

# Shipping (InPost/DPD)
INPOST_API_KEY=""
DPD_API_KEY=""

# Invoicing
WFIRMA_API_KEY=""

# Email (SendGrid/SMTP)
EMAIL_HOST=""
EMAIL_PORT=587
EMAIL_USER=""
EMAIL_PASSWORD=""
EMAIL_FROM="noreply@amgsquant.pl"

# Environment
NODE_ENV="development"
```

KROK 10: Stwórz .cursorrules
```
# AMGSquant Print System - Cursor AI Rules

## Project Context
This is a monorepo project for a large-format printing house management system.
- E-commerce frontend with product configurators
- Backend API with production queue management
- Real-time pricing engine
- File validation (preflight)
- Warehouse management

## Tech Stack
- **Frontend**: Next.js 14 (App Router), TypeScript, Tailwind CSS, Zustand
- **Backend**: NestJS, Prisma, PostgreSQL, Redis, RabbitMQ
- **Monorepo**: pnpm workspaces + Turbo

## Code Style
- Use TypeScript strict mode
- Prefer functional components and hooks
- Use async/await over promises
- Error handling with try-catch
- Always add JSDoc comments for public methods
- Follow ESLint rules

## File Organization
- Group by feature, not by file type
- Keep components small and focused
- Separate business logic from UI
- Use index.ts for clean imports

## Naming Conventions
- PascalCase for components and classes
- camelCase for functions and variables
- UPPER_SNAKE_CASE for constants
- kebab-case for file names

## Best Practices
- Write unit tests for business logic
- Use DTOs for API validation
- Implement proper error handling
- Add loading and error states
- Optimize database queries
- Cache frequently accessed data in Redis

## Security
- Never commit secrets or API keys
- Validate all user inputs
- Use parameterized queries
- Implement rate limiting
- Use HTTPS in production

## Performance
- Lazy load images and components
- Implement pagination for large lists
- Use React Query for data fetching
- Optimize bundle size
- Use CDN for static assets
```

KROK 11: Commit i push
```bash
git add .
git commit -m "feat: initialize project structure for print management system"
git push -u origin develop
```

Wykonaj wszystkie kroki powyżej dokładnie w podanej kolejności. Raportuj postęp po każdym kroku.

## Next Steps

1. Review the task instructions above
2. Use Cursor's AI features (Cmd+K or Cmd+L) to implement
3. Test your changes
4. When complete, mark this task as done in the orchestrator

---
*This task was created by Claude-Cursor Orchestrator*
